{
	"$jsonSchema": {
		"bsonType": "object",
		"required": ["loginId", "passwordHash", "role", "name"],
		"properties": {
			"loginId": { "bsonType": "string", "minLength": 1 },
			"passwordHash": { "bsonType": "string", "minLength": 20 },
			"role": { "bsonType": "string", "enum": ["admin", "user"] },
			"name": { "bsonType": "string", "minLength": 1 },
			"createdAt": { "bsonType": ["date", "null"] },
			"updatedAt": { "bsonType": ["date", "null"] }
		}
	}
}
