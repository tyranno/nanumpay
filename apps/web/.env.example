# MongoDB 연결 문자열
MONGODB_URI=mongodb://localhost:27017/nanumpay

# JWT 시크릿 키 (운영 환경에서는 반드시 변경하세요)
JWT_SECRET=your-secret-key-here-change-in-production

# JWT 만료 시간 (예: 1h, 30m, 7d)
JWT_EXPIRES=1h
JWT_REFRESH_EXPIRES=7d

# 서버 포트
PORT=3000

# 노드 환경
NODE_ENV=development