// Capacitor auto-generated build file

android {
    sourceSets {
        main {
            assets {
                srcDirs += "../node_modules/@capacitor/android/capacitor/src/main/assets"
            }
        }
    }
}

def capacitorConfig = rootProject.file("capacitor.config.json").text
def config = new groovy.json.JsonSlurper().parseText(capacitorConfig)

dependencies {
    implementation project(':capacitor-android')
    implementation project(':capacitor-preferences')
}

if (hasProperty('postBuildExtras')) {
    postBuildExtras()
}